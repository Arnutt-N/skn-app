# คู่มือการใช้งาน Skill Seekers สำหรับผู้ใช้ทั่วไป

> **สร้าง Skill ให้ AI จากเอกสารและเว็บไซต์ได้ง่าย ๆ ด้วย MCP Context7**

---

## 📖 สารบัญ

1. [Skill Seekers คืออะไร](#-skill-seekers-คืออะไร)
2. [MCP Context7 คืออะไร](#-mcp-context7-คืออะไร)
3. [ติดตั้งและตั้งค่า](#-ติดตั้งและตั้งค่า)
4. [วิธีใช้งานพื้นฐาน](#-วิธีใช้งานพื้นฐาน)
5. [ตัวอย่างการใช้งานจริง](#-ตัวอย่างการใช้งานจริง)
6. [แก้ไขปัญหาที่พบบ่อย](#-แก้ไขปัญหาที่พบบ่อย)

---

## 🤔 Skill Seekers คืออะไร

**Skill Seekers** เป็นเครื่องมือที่ช่วยแปลงเว็บไซต์เอกสาร, GitHub Repository, หรือไฟล์ PDF ให้กลายเป็น **AI Skill** ที่ใช้งานได้กับ Claude AI และ AI อื่น ๆ

### ทำไมต้องใช้ Skill Seekers?

| ปัญหาเดิม | แก้ไขด้วย Skill Seekers |
|-----------|------------------------|
| ต้องอ่านเอกสารหลายร้อยหน้าเอง | AI อ่านและสรุปให้ |
| หาข้อมูลในเอกสารไม่เจอ | ถาม AI ได้เลย |
| ต้องสร้าง Skill เองด้วยมือ | สร้างอัตโนมัติใน 20-40 นาที |

### ทำอะไรได้บ้าง?

- ✅ **ดึงข้อมูลจากเว็บเอกสาร** - รองรับทุกเว็บไซต์เอกสาร
- ✅ **วิเคราะห์โค้ดจาก GitHub** - ดึงโค้ดและวิเคราะห์โครงสร้าง
- ✅ **แปลงไฟล์ PDF** - แม้แต่ PDF ที่สแกนก็อ่านได้
- ✅ **ตรวจจับข้อผิดพลาด** - เปรียบเทียบเอกสารกับโค้ดจริง

---

## 🔌 MCP Context7 คืออะไร

**MCP (Model Context Protocol)** คือมาตรฐานที่ช่วยให้ AI เข้าใจบริบทและข้อมูลได้ดีขึ้น

**Context7** เป็นส่วนหนึ่งของ MCP ที่ช่วยให้ AI สามารถ:
- เข้าถึงเอกสารอ้างอิงที่อัพเดตล่าสุด
- เข้าใจโครงสร้างโค้ดและ API
- ตอบคำถามได้แม่นยำขึ้น

### ประโยชน์ของการใช้ MCP Context7

```
ก่อนใช้ MCP:
คุณ: "ฟังก์ชัน move_local_x ทำงานยังไง?"
AI: "ขออภัย ฉันไม่มีข้อมูลเกี่ยวกับฟังก์ชันนี้"

หลังใช้ MCP:
คุณ: "ฟังก์ชัน move_local_x ทำงานยังไง?"
AI: "ฟังก์ชัน move_local_x(delta: float, snap: bool = False) ใช้สำหรับ
     เลื่อนตำแหน่งตามแกน X โดยมีพารามิเตอร์:
     - delta: ระยะทางที่ต้องการเลื่อน
     - snap: ถ้าเป็น True จะจัดตำแหน่งให้ชิดกริด"
```

---

## 📥 ติดตั้งและตั้งค่า

### ขั้นตอนที่ 1: ตรวจสอบความต้องการของระบบ

**ต้องการ:**
- คอมพิวเตอร์ (Windows, macOS, หรือ Linux)
- Python 3.10 หรือสูงกว่า
- อินเทอร์เน็ต

**ตรวจสอบ Python:**

```bash
# Windows
python --version

# macOS/Linux
python3 --version
```

> ถ้าแสดง `Python 3.10.x` หรือสูงกว่า → ผ่าน! ไปขั้นตอนถัดไป
> ถ้าไม่มี → ต้องติดตั้ง Python ก่อน (ดูที่ [python.org](https://python.org))

---

### ขั้นตอนที่ 2: ติดตั้ง Skill Seekers

#### วิธีที่ 1: ติดตั้งจาก PyPI (แนะนำ)

```bash
# ติดตั้งเวอร์ชันพื้นฐาน
pip install skill-seekers

# หรือติดตั้งพร้อม MCP
pip install skill-seekers[mcp]

# หรือติดตั้งทุกฟีเจอร์
pip install skill-seekers[all]
```

#### วิธีที่ 2: ติดตั้งจาก GitHub

```bash
# 1. โคลนโปรเจค
git clone https://github.com/yusufkaraaslan/Skill_Seekers.git
cd Skill_Seekers

# 2. สร้าง Virtual Environment
python3 -m venv venv

# 3. เปิดใช้งาน Virtual Environment
# macOS/Linux:
source venv/bin/activate

# Windows:
venv\Scripts\activate

# 4. ติดตั้ง
pip install -e .
```

---

### ขั้นตอนที่ 3: ตั้งค่า MCP Server

#### สำหรับ Claude Code (แนะนำ)

**Windows:**
```powershell
# รัน script ตั้งค่าอัตโนมัติ
.\setup_mcp.sh
```

**macOS/Linux:**
```bash
# รัน script ตั้งค่าอัตโนมัติ
./setup_mcp.sh
```

หรือตั้งค่าด้วยตนเอง:

```bash
# สร้างไฟล์ตั้งค่า
nano ~/.claude.json
```

ใส่เนื้อหานี้:

```json
{
  "mcpServers": {
    "skill-seeker": {
      "type": "stdio",
      "command": "python3",
      "args": ["-m", "skill_seekers.mcp.server_fastmcp"]
    }
  }
}
```

#### สำหรับ AI Tools อื่น ๆ

| AI Tool | วิธีตั้งค่า |
|---------|------------|
| **Cursor** | ตั้งค่า HTTP server ที่ port 3000 |
| **Windsurf** | ตั้งค่า HTTP server ที่ port 3001 |
| **VS Code + Cline** | ใช้ stdio transport เหมือน Claude Code |
| **IntelliJ IDEA** | ตั้งค่า HTTP server ที่ port 3002 |

---

### ขั้นตอนที่ 4: เริ่มต้น HTTP Server (สำหรับ Cursor, Windsurf, IntelliJ)

```bash
# เริ่ม server ที่ port 3000
python -m skill_seekers.mcp.server_fastmcp --http --port 3000

# หรือใช้ port อื่น
python -m skill_seekers.mcp.server_fastmcp --http --port 8080
```

**ตรวจสอบว่าทำงานถูกต้อง:**
```bash
curl http://localhost:3000/health
# ควรแสดง: {"status": "ok"}
```

---

### ขั้นตอนที่ 5: รีสตาร์ท AI Agent

หลังตั้งค่าเสร็จ ให้ปิดและเปิด AI agent ใหม่ เพื่อให้โหลด MCP tools

---

## 🎮 วิธีใช้งานพื้นฐาน

### การใช้งานผ่าน MCP (พูดเป็นภาษาธรรมชาติ)

หลังตั้งค่า MCP เสร็จ คุณสามารถสั่งงาน AI ได้โดยตรง:

#### 1. ดูรายการ Config ที่มีอยู่

```
คุณ: List all available configs

AI: มี configs ที่ใช้ได้ดังนี้:
     1. godot - Godot Engine documentation
     2. react - React framework
     3. vue - Vue.js framework
     4. django - Django web framework
     5. fastapi - FastAPI Python framework
     ...
```

#### 2. สร้าง Config ใหม่

```
คุณ: Generate a config for Tailwind CSS at https://tailwindcss.com/docs

AI: ✅ Config สร้างเสร็จแล้ว: configs/tailwind.json
```

#### 3. ประเมินจำนวนหน้า

```
คุณ: Estimate pages for configs/react.json

AI: 📊 ประมาณการ:
     - จำนวนหน้า: 230 หน้า
     - เวลาที่ใช้: ประมาณ 2 นาที
     - แนะนำ max_pages: 280
```

#### 4. ดึงข้อมูลเอกสาร

```
คุณ: Scrape docs using configs/react.json

AI: ⏳ กำลังดึงข้อมูล...
     [==================> ] 180/230 หน้า
     
     ✅ เสร็จสมบูรณ์!
     📁 บันทึกที่: output/react/
```

#### 5. ปรับปรุงคุณภาพ Skill

```
คุณ: Enhance skill at output/react/

AI: ✨ กำลังปรับปรุง SKILL.md...
     - เพิ่มตัวอย่างโค้ด
     - เพิ่มคำอธิบายเชิงลึก
     - จัดระเบียบเนื้อหา
     
     ✅ ปรับปรุงเสร็จแล้ว!
```

#### 6. แพ็คเกจ Skill

```
คุณ: Package skill at output/react/

AI: 📦 กำลังสร้างไฟล์...
     ✅ สร้างเสร็จ: output/react.zip
     📏 ขนาด: 245 KB
     
     พร้อมอัปโหลดไป Claude!
```

#### 7. อัปโหลดอัตโนมัติ (ถ้ามี API Key)

```
คุณ: Upload output/react.zip

AI: ☁️ กำลังอัปโหลด...
     ✅ อัปโหลดเสร็จแล้ว!
     🆔 Skill ID: react_20250115_143022
```

---

### การใช้งานผ่าน Command Line

#### คำสั่งพื้นฐาน

```bash
# ดึงข้อมูลเอกสาร
skill-seekers scrape --config configs/react.json

# ดึงข้อมูลแบบโต้ตอบ
skill-seekers scrape --interactive

# ดึงข้อมูลแบบระบุ URL ตรง ๆ
skill-seekers scrape --name myframework --url https://docs.example.com/

# ดึงข้อมูลจาก GitHub
skill-seekers github --repo facebook/react

# ดึงข้อมูลจาก PDF
skill-seekers pdf --pdf docs/manual.pdf --name myskill

# ดึงข้อมูลแบบรวมหลายแหล่ง
skill-seekers unified --config configs/react_unified.json
```

#### คำสั่งปรับปรุงและแพ็คเกจ

```bash
# ปรับปรุงคุณภาพ (แบบ local - ไม่ต้องใช้ API key)
skill-seekers enhance output/react/

# แพ็คเกจสำหรับ Claude (ค่าเริ่มต้น)
skill-seekers package output/react/

# แพ็คเกจสำหรับ Google Gemini
skill-seekers package output/react/ --target gemini

# แพ็คเกจสำหรับ OpenAI
skill-seekers package output/react/ --target openai

# อัปโหลดอัตโนมัติ
skill-seekers upload output/react.zip
```

#### คำสั่งทำงานเต็มรูปแบบ (One-Command)

```bash
# ทำทุกขั้นตอนในคำสั่งเดียว
skill-seekers install --config react

# ไม่อัปโหลด (แค่แพ็คเกจ)
skill-seekers install --config react --no-upload

# ไม่จำกัดจำนวนหน้า
skill-seekers install --config godot --unlimited
```

---

## 📚 ตัวอย่างการใช้งานจริง

### ตัวอย่างที่ 1: สร้าง Skill จากเอกสาร React

```bash
# ขั้นตอนที่ 1: ประเมินขนาด
skill-seekers estimate configs/react.json

# ขั้นตอนที่ 2: ดึงข้อมูลพร้อมปรับปรุง
skill-seekers scrape --config configs/react.json --enhance-local

# ขั้นตอนที่ 3: แพ็คเกจ
skill-seekers package output/react/

# ขั้นตอนที่ 4: อัปโหลด (ถ้ามี API Key)
skill-seekers upload output/react.zip
```

**เวลาที่ใช้:** ประมาณ 25-40 นาที  
**ผลลัพธ์:** Skill ที่พร้อมใช้งานกับ Claude

---

### ตัวอย่างที่ 2: สร้าง Skill จาก GitHub Repository

```bash
# ดึงข้อมูลจาก repo บน GitHub
skill-seekers github --repo facebook/react

# ดึงพร้อม Issues และ Releases
skill-seekers github --repo django/django \
    --include-issues \
    --max-issues 100 \
    --include-changelog \
    --include-releases
```

---

### ตัวอย่างที่ 3: สร้าง Skill จากไฟล์ PDF

```bash
# PDF ทั่วไป
skill-seekers pdf --pdf docs/manual.pdf --name myskill

# PDF ที่มีตาราง
skill-seekers pdf --pdf docs/manual.pdf --name myskill --extract-tables

# PDF ที่สแกน (ต้องติดตั้ง pytesseract)
skill-seekers pdf --pdf docs/scanned.pdf --name myskill --ocr

# PDF ที่มีรหัสผ่าน
skill-seekers pdf --pdf docs/encrypted.pdf --name myskill --password mypassword
```

---

### ตัวอย่างที่ 4: รวมหลายแหล่งข้อมูล (Multi-Source)

```bash
# สร้างไฟล์ config แบบรวม
cat > configs/myproject_unified.json << 'EOF'
{
  "name": "myproject",
  "description": "Complete framework knowledge from docs + code",
  "merge_mode": "rule-based",
  "sources": [
    {
      "type": "documentation",
      "base_url": "https://docs.myframework.com/",
      "max_pages": 200
    },
    {
      "type": "github",
      "repo": "owner/myframework",
      "include_code": true
    }
  ]
}
EOF

# รัน
skill-seekers unified --config configs/myproject_unified.json
```

**ประโยชน์:** ตรวจจับความไม่ตรงกันระหว่างเอกสารกับโค้ดจริง

---

### ตัวอย่างที่ 5: ใช้งานผ่าน MCP แบบเต็มรูปแบบ

```
คุณ: Generate config for Vue.js at https://vuejs.org/guide/

AI: ✅ Config สร้างเสร็จ: configs/vue.json

---

คุณ: Estimate pages for configs/vue.json

AI: 📊 ประมาณ 180 หน้า

---

คุณ: Scrape docs using configs/vue.json

AI: ⏳ กำลังดึงข้อมูล 180 หน้า...
     [==================> ] 95%
     ✅ เสร็จแล้ว! บันทึกที่ output/vue/

---

คุณ: Enhance skill at output/vue/

AI: ✨ ปรับปรุงเสร็จแล้ว!

---

คุณ: Package skill at output/vue/

AI: 📦 สร้างไฟล์: output/vue.zip

---

คุณ: Upload output/vue.zip

AI: ☁️ อัปโหลดเสร็จแล้ว!
```

---

## 🔧 แก้ไขปัญหาที่พบบ่อย

### ปัญหา: "command not found: skill-seekers"

**สาเหตุ:** ยังไม่ได้ติดตั้งหรือ virtual environment ไม่ได้เปิด

**แก้ไข:**
```bash
# เปิด virtual environment
cd ~/Skill_Seekers
source venv/bin/activate  # macOS/Linux
# หรือ
venv\Scripts\activate  # Windows

# ติดตั้งใหม่
pip install -e .
```

---

### ปัญหา: "ModuleNotFoundError: No module named 'skill_seekers'"

**สาเหตุ:** Package ไม่ได้ติดตั้ง

**แก้ไข:**
```bash
pip install -e .
```

---

### ปัญหา: MCP Server ไม่ทำงาน

**ตรวจสอบ:**
```bash
# ทดสอบ server
python3 -m skill_seekers.mcp.server_fastmcp --help

# ตรวจสอบ JSON config
python3 -m json.tool ~/.claude.json
```

**แก้ไข:**
1. ตรวจสอบว่า Python path ถูกต้อง
2. ตรวจสอบว่า JSON ไฟล์ไม่มี error
3. รีสตาร์ท Claude Code

---

### ปัญหา: "Rate limit exceeded" จาก GitHub

**สาเหตุ:** GitHub จำกัดจำนวนการเรียก API

**แก้ไข:**
```bash
# ตั้งค่า GitHub Token
export GITHUB_TOKEN=ghp_your_token_here

# หรือใช้คำสั่ง config
skill-seekers config --github
```

---

### ปัญหา: ดึงข้อมูลช้าหรือค้าง

**แก้ไข:**
```bash
# ลดจำนวนหน้า
skill-seekers scrape --config configs/react.json --max-pages 50

# หรือใช้ async mode
skill-seekers scrape --config configs/react.json --async --workers 4

# ตรวจสอบการเชื่อมต่อ
ping google.com
```

---

### ปัญหา: HTTP Server ไม่สามารถเริ่มได้

**สาเหตุ:** Port ถูกใช้งานแล้ว

**แก้ไข:**
```bash
# หา process ที่ใช้ port
lsof -i :3000  # macOS/Linux
netstat -ano | findstr :3000  # Windows

# หรือใช้ port อื่น
python -m skill_seekers.mcp.server_fastmcp --http --port 3001
```

---

## 📋 สรุปคำสั่งที่ใช้บ่อย

| งานที่ต้องการ | คำสั่ง |
|--------------|--------|
| ดึงข้อมูลเอกสาร | `skill-seekers scrape --config configs/name.json` |
| ดึงข้อมูล GitHub | `skill-seekers github --repo owner/repo` |
| ดึงข้อมูล PDF | `skill-seekers pdf --pdf file.pdf --name skillname` |
| รวมหลายแหล่ง | `skill-seekers unified --config configs/name_unified.json` |
| ปรับปรุงคุณภาพ | `skill-seekers enhance output/name/` |
| แพ็คเกจ | `skill-seekers package output/name/` |
| อัปโหลด | `skill-seekers upload output/name.zip` |
| ทำทั้งหมดในคำสั่งเดียว | `skill-seekers install --config name` |

---

## 💡 เคล็ดลับการใช้งาน

1. **เริ่มจากน้อย** - ทดสอบด้วย `max_pages: 20` ก่อน
2. **ประเมินก่อน** - ใช้ `estimate` เพื่อรู้เวลาที่ใช้
3. **ใช้ cache** - ถ้าดึงข้อมูลไปแล้ว ใช้ `--skip-scrape` เพื่อประหยัดเวลา
4. **ปรับปรุงเสมอ** - ใช้ `enhance` เพื่อให้ Skill มีคุณภาพดีขึ้น
5. **ทดสอบ dry-run** - ใช้ `--dry-run` เพื่อตรวจสอบก่อนดึงจริง

---

## 🔗 แหล่งข้อมูลเพิ่มเติม

- **GitHub Repository:** https://github.com/yusufkaraaslan/Skill_Seekers
- **เอกสารเต็ม:** https://skillseekersweb.com/
- **รายงานปัญหา:** https://github.com/yusufkaraaslan/Skill_Seekers/issues
- **MCP Documentation:** https://modelcontextprotocol.io/

---

## 📝 หมายเหตุ

- เอกสารนี้เขียนสำหรับ Skill Seekers version 2.7.4
- ต้องการ Python 3.10 หรือสูงกว่า
- บางฟีเจอร์ต้องใช้ API Key (Claude, Gemini, OpenAI)

---

**สร้างเมื่อ:** กุมภาพันธ์ 2026  
**เขียนโดย:** AI Assistant สำหรับผู้ใช้ทั่วไป

**ขอให้สนุกกับการสร้าง Skill! 🚀**
